<% if @view.locals[:team] == nil %>
  <td>&nbsp;</td>
<% else %>
  <td><%= link_to @view.locals[:team].name,
              team_path(@view.locals[:team]),
              title: "Team added #{@view.locals[:team].created_at.strftime("%a, %d %b %Y")}" %>
  </td>
<% end %>
