$(document).ready(function(){
  //alert('doc loaded for page name ' + __view.name)

  // Popovers must be initialized with jQuery: select the specified element
  // and call the popover() method.
  // per: http://www.w3schools.com/bootstrap/bootstrap_popover.asp
  $('[data-toggle="popover"]').popover();

  // end: on doc.ready
});
